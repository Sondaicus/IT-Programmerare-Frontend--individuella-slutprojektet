

export default function TechniquePick(techIdentifyer : string, techValues)
{
    let openParam : string = "<i>(";
    let closingParam : string = ")</i>";

    return (
        <div>
        </div>
    );
}